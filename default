server {
    listen 80;

	if ($host ~ /^www\./) {
		location {
			rewrite ^ $2.$3.$4/$request_uri last;
		}		
	}
}

server {
    listen 443 ssl;
    
	if ($host ~ /^www\./) {
		location {
			rewrite ^ $2.$3.$4/$request_uri last;
		}		
	}
}